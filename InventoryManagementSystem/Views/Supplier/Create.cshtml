@model InventoryManagementSystem.Models.Supplier
@section Title {
        Add Supplier
}
<div class="row justify-content-center mt-4">
    <div class="col-md-8">
        <div class="card shadow-sm">
            <div class="card-header bg-primary text-white">
                <h4 class="mb-0">Add New Supplier</h4>
            </div>
            <div class="card-body">
                <form asp-action="Create" method="post">
                    @Html.AntiForgeryToken()
                    <div class="row">
                        <div class="col-md-12 form-group mb-3">
                            <label asp-for="Name" class="form-label fw-bold">Supplier Name</label>
                            <input asp-for="Name" class="form-control" placeholder="e.g., Acme Distribution" required />
                        </div>
                        <div class="col-md-6 form-group mb-3">
                            <label asp-for="ContactAddress" class="form-label fw-bold">Address</label>
                            <input asp-for="ContactAddress" class="form-control" placeholder="No.6 Jalan Sri Siantan" required />
                        </div>
                        <div class="col-md-6 form-group mb-3">
                            <label asp-for="PhoneNumber" class="form-label fw-bold">Phone Number</label>
                            <input asp-for="PhoneNumber" class="form-control" placeholder="e.g., 012-3456789" required />
                        </div>
                    </div>
                    <div class="d-flex justify-content-between mt-2">
                        <a asp-action="Index" class="btn btn-secondary">Back to List</a>
                        <button type="submit" class="btn btn-primary px-4">Save Supplier</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>